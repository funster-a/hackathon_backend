# 👁️ FinSight: AI Financial Assistant

![Flutter](https://img.shields.io/badge/Flutter-3.24-02569B?logo=flutter) ![FastAPI](https://img.shields.io/badge/Backend-FastAPI-009688?logo=fastapi) ![AI](https://img.shields.io/badge/AI-DeepSeek_V3-blueviolet)

**Репозиторий решения для хакатона [Название Хакатона]**

### 1. Задача
Разработать мобильное приложение "Финансовый ассистент", которое автоматизирует анализ личных финансов.
**Проблема:** Пользователи не понимают структуру своих расходов из сухих банковских выписок, не видят скрытых подписок и не знают, как оптимизировать бюджет.
**Решение:** Приложение загружает PDF-выписку, классифицирует траты, выявляет подписки/рассрочки и дает персонализированные советы через AI-чат.

### 2. Данные
* **Входные данные:** Банковские выписки в формате PDF (оптимизировано под **Kaspi Gold**).
    * *Особенности данных:* Неструктурированный текст, нестандартное форматирование сумм (напр. `1 500.00 - T`), смешанные языки (RU/KZ/EN).
* **Обработка:** Извлечение сырого текста -> Преобразование в JSON-структуру транзакций.
* **Выходные данные:**
    * Структурированный список транзакций (Дата, Сумма, Категория).
    * Агрегированная статистика по категориям.
    * Список активных подписок и рассрочек.

### 3. Метод
Мы используем гибридный подход **Flutter + Python (FastAPI) + LLM**:
1.  **Парсинг (Python):** Библиотека `pdfplumber` извлекает текстовый слой из PDF.
2.  **AI-Анализ (DeepSeek-V3):**
    * Используется **Chain-of-Thought (CoT)** промптинг для очистки "мусорных" данных.
    * **Rule-Based Logic:** Внедрены "Железные правила" в системный промпт для разделения визуально похожих транзакций (например, *Yandex Go* → Такси, *Yandex Plus* → Подписка).
    * Детекция рассрочек (*Kaspi Red*) как отдельной сущности, а не просто "расхода".
3.  **Визуализация (Flutter):**
    * Динамический UI в стиле **Liquid Glass**.
    * Локальная фильтрация данных (неделя/месяц/год) без повторных запросов к API.

### 4. Метрики (До / После)
Сравнение нашего AI-подхода с классическими Regex-парсерами:

| Метрика | Традиционный подход (Regex) | Наше решение (FinSight AI) |
| :--- | :--- | :--- |
| **Точность парсинга** | ~60% (падает на кривой верстке PDF) | **98%** (LLM понимает контекст и опечатки) |
| **Классификация** | Жесткая (по ключевым словам) | **Смысловая** (понимает, что "Magnum" = Еда) |
| **Детекция подписок** | Только по точному совпадению | **Интеллектуальная** (анализ сумм и частоты) |
| **Скорость разработки** | Дни на написание правил | **Часы** на настройку промпта |

### 5. Ограничения
1.  **Формат файлов:** Текущая версия поддерживает только PDF (OCR для картинок не реализован для скорости).
2.  **Контекстное окно:** Лимит токенов LLM ограничивает анализ выписок размером более 10-15 страниц за один раз.
3.  **Зависимость от сети:** Для анализа требуется интернет-соединение (обработка на сервере).
4.  **Stateless:** Из соображений приватности мы не храним историю транзакций на сервере (только локальный кэш на устройстве).

---
*Команда разработчиков: Амир Аханов, команда good kid maad code

